fastapi
uvicorn[standard]
pydantic[email]
pydantic-settings
sqlalchemy[asyncio]
asyncpg
aiosqlite
alembic
PyJWT
bcrypt
python-multipart
python-dotenv
httpx
spacy
numpy
pandas
slowapi
pytest
pytest-asyncio
pytest-cov
anyio
# ── ClinicalBERT embedding layer ────────────────────────────────────────────────────────────
# Primary path uses HuggingFace Inference API (set HF_API_TOKEN env var).
# No local model install needed on Render — httpx (already listed above) handles it.
# Uncomment the lines below ONLY if you want to run the model locally (GPU server):
# torch --index-url https://download.pytorch.org/whl/cpu
# transformers>=4.40.0
scikit-learn
